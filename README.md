# Instructions Writer

A Python project for writing instructions using Gemini via Vertex AI.

```
poetry install
poetry run instructions-writer <folder_path>
poetry run instructions-writer --dry-run <folder_path>
```
